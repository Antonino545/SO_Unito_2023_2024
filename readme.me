docker build --platform linux/amd64 -t so-2024 .
per effettuare il build dell'immagine docker in modo da poterla eseguire su architettura x86_64 (amd64)
Per eseguire l'immagine Docker 
docker run so-2024